const LOCALIZE = {
    US: {
        AnnouncementBarContent:'Replay your tests as they occurred in your CI run and debug with confidence using Test Replay in Cypress Cloud!',
    }
};

export default LOCALIZE;
